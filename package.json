{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev": "bash scripts/dev.sh",
    "start": "bash scripts/start.sh",
    "dev:api": "bash scripts/with-env.sh pnpm --filter @workspace/api-server run dev",
    "dev:desktop": "pnpm --filter @workspace/fieldpress-desktop run dev",
    "dev:mobile": "pnpm --filter @workspace/fieldpress run dev",
    "db:push": "bash scripts/with-env.sh pnpm --filter @workspace/db run push"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.4",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
