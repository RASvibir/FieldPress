import React from 'react';
import ReactDOM from 'react-dom/client';
import FieldPressMaster from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FieldPressMaster />
  </React.StrictMode>
);
