# Apps mapping

Physical `apps/` moves wait until Vercel paths can change in one cutover.

| Target | Current path |
| --- | --- |
| `apps/web` | `artifacts/fieldpress-desktop` |
| `apps/api` | `artifacts/api-server` |
| `apps/desktop` | `src-tauri` + desktop Vite shell |

Public URL for all of them: https://fieldpress.studio
